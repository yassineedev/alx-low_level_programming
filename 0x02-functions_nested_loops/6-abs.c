#include "main.h"
/**
 * _abs - computed function that give us absulet value
 * @n: an int
 * Return: return n if is great than 0 & return -n if is less than 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
