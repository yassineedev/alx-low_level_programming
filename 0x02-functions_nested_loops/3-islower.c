#include "main.h"
/**
 * _islower - prints lower cases
 *  Return: Return 1 if is lower case and return 0 if it it's not
 */
int _islower(int c)
{
	for (c = 97; c <=122; c++)
		return (1);
	return (0);
}
