#include <stdio.h>
#include <stdlib.h>
/**
 * main - main is main
 * des of my code
 * Return: always )
 */
int main(void)
{
	int n;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}

		putchar('\n');
		return (0);
}
