#include <stdio.h>
#include <stdlib.h>
/**
* main - main is main
* des for my code
* Return: always 0 success
*/
int main(void)
{
	int f, s;
		for (f = 97; f <= 122 ; f++)
	{
		putchar(f);
	}
		for (s = 65; s <= 90 ; s++)
		{
			putchar(s);
		}
			putchar('\n');
			return (0);
}
