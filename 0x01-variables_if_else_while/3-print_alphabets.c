#include <stdio.h>
#include <stdlib.h>
/**
 * main-The main function
 * Description: This is nothing
 * Return: always 0 (success)
 */
int main(void)
{
	int n, g;
		for (n = 97; n <= 122; n++)
	{
		putchar(n);
	}
		for (g = 65; g <= 90; g++)
		{
			putchar(g);
		}
			putchar(10);
			return (0);
}
