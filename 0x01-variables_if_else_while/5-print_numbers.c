#include <stdio.h>
#include <stdlib.h>
/**
 * main-The main function
 * Description: This is nothing
 * Return: always 0 (success)
 */
int main(void)
{
	int n;
		for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	return (0);
}
