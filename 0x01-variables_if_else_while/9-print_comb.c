#include <stdio.h>
/**
 * main - main is main
 * Return: always 0
 */
int main(void)
{
	int a;

	for (a = 48; a <= 56; a++)
	{
		putchar(a);
		putchar(',');
		putchar(' ');
	}
		putchar('9');
		putchar('\n');
		return (0);
}
