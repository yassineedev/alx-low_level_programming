#include <stdio.h>
#include <stdlib.h>
/**
 * main-The main function
 * Description: This is nothing
 * Return: always 0 (success)
 */
int main(void)
{
	int n;
		for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	int b;
		for (b = 97; b <= 102; b++)
	{
		putchar(b);
	}
		putchar(10);
	return (0);
}
